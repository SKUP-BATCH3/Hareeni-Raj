#include<stdio.h>
void arbitration(int size,int msg);
void main()
{
    int i,a,size,msg;
int msg1(msg),msg2(msg);
while(a<size)
{
    printf("enter the id for msg1");
    for(i=0;i<msg;i++)
    {
        scanf("%d",&msg1[i]);
    }
    a++;
}
printf("enter the id for msg1");
    for(i=0;i<msg;i++)
        {
        scanf("%d",&msg2[i]);
    }
}
{
    if (msg1[i]==1 & msg2[i]==0)|(msg1[i]==0 & msg2[i]==1)
    {
        printf("msg id won the arbitration");
    }
    else
        printf("msg id lost the arbitration");
}
{
int i,arr,a;
return 0;
}
